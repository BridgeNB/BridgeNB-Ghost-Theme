{{!< layout}}

<h2>标签： {{tag.name}}</h2>
<hr/>

{{#foreach posts}}
    <article>
      <header class="post-header">
          <h2 class="post-title"><a href="{{url}}">{{title}}</a></h2>
      </header>
    </article>
{{/foreach}}

{{pagination}}>
